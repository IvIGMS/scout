docker run -d --name=postgres_new -p 5433:5432 -v postgres_new_volume:/var/lib/postgresql/data -e POSTGRES_PASSWORD=postgres postgres

select * from words_frequency order by frequency desc;

select * from public.videos;


CREATE TABLE videos (
  length INTEGER,
  views INTEGER,
  url VARCHAR,
  name VARCHAR,
  author VARCHAR
);

CREATE TABLE words_frequency (
  frequency INTEGER,
  word VARCHAR
);
